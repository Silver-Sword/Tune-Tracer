{
  "name": "frontend",
  "version": "0.0.1",
  "description": "frontend components of tune tracer",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "tsc && pnpm start",
    "debug": "node --inspect-brk -r ts-node/register src/main.ts"
  },
  "dependencies": {
    "typescript": "^5.5.4"
  }
}
