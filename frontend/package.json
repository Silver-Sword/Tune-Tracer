{
  "name": "frontend",
  "version": "0.0.0",
  "description": "frontend components of tune tracer",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
